#include "main.h"
#include <stdio.h>
/**
 * mul - Multiplies two integers
 * @a: integer to be checked
 * @b: integer to be checked
 * Return: result
 */

int mul(int a, int b)
{
	int a, b, result;

	result = (a * b);

	return (result);
}
